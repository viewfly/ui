---
description: Viewfly 组件：简单状态用 createSignal，多字段状态用 reactive
globs: packages/**/*.tsx
alwaysApply: false
---

# Viewfly：`createSignal` / `reactive`（摘要）

- **单一标量/布尔/字符串**：优先 **`createSignal`**。
- **多字段成组 UI 状态**（如 `{ open, x, y }`、表单草稿）：优先 **`reactive`**，避免一堆相关 signal 与成组更新抖动。
- 字段彼此独立时，多个 `createSignal` 仍合理；不要为单个值包一层 `reactive({ value })`。
- 从 `@viewfly/core` 按需导入，与现有组件一致。

**完整说明与正/反例**：[docs/agent-guidelines.md#viewfly-state-signal-reactive](../../docs/agent-guidelines.md#viewfly-state-signal-reactive)。
