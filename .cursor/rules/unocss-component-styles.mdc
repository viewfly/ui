---
description: 组件样式使用 UnoCSS、独立 SCSS、语义化 class（vfui- 前缀 BEM）
globs: packages/**/*.{tsx,scss}
alwaysApply: false
---

# 组件样式 UnoCSS + SCSS（摘要）

- 视觉样式以 **UnoCSS** 为主；组件旁独立 **SCSS**，用 **`@apply`** 组合原子类；TSX 不写 `<style>` 或大块内联样式（动态值除外）。
- 类名：**语义稳定**，推荐 **`vfui-` + BEM**（`vfui-块`、`vfui-块__元素`、`vfui-块--修饰符`）；主样式不要堆在 TSX 字符串里。
- `packages/components` 已配 **`unocss/vite`** 与 **`transformerDirectives`**，构建后不应残留未展开的 `@apply` 依赖问题（按现有 uno 配置维护）。

**完整说明与示例**：[docs/agent-guidelines.md#unocss-component-styles](../../docs/agent-guidelines.md#unocss-component-styles)。
