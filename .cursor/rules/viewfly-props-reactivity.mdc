---
description: Viewfly 组件：主体内不解构 props，仅在返回的渲染函数内解构
globs: packages/**/*.tsx
alwaysApply: false
---

# Viewfly：Props 响应性（摘要）

- **`props` 是响应式对象**：在**组件主函数体**（`return () => ...` 之外）**不要**解构或展开 `props`。
- 在**组件返回的渲染函数**内再 `const { ... } = props` 读取默认值与字段。
- 主体内若故意做「一次性非响应快照」，须在评审中说明；默认按上述规则。

**完整说明与示例**：[docs/agent-guidelines.md#viewfly-props-reactivity](../../docs/agent-guidelines.md#viewfly-props-reactivity)。
