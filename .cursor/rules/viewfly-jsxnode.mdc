---
description: Viewfly：可渲染子节点类型用 JSXNode，不用 JSX.Element
globs: packages/**/*.tsx
alwaysApply: false
---

# Viewfly：`JSXNode`（摘要）

- 子节点、插槽、任意可渲染内容：**类型用 `JSXNode`**（`import type { JSXNode } from '@viewfly/core'`）。
- **不要用 `JSX.Element`** 表示上述内容（过窄，与框架 `children` 类型不一致）。

**完整说明与示例**：[docs/agent-guidelines.md#viewfly-jsxnode](../../docs/agent-guidelines.md#viewfly-jsxnode)。
