---
description: Viewfly 用依赖注入（createContextProvider + inject）做跨组件通信及与 Signal 的配合
globs: packages/**/*.tsx
alwaysApply: false
---

# Viewfly：依赖注入（摘要）

- 祖先/后代共享状态优先 **`createContextProvider` + `InjectionToken` + `inject`**；消费方在**主函数体**内 `inject(token, notFound?)`，可选依赖用 `notFound` 即可。
- **`withAnnotation` 的 providers**：当前组件 `setup` 主体内也可 `inject`；`createContextProvider` 的上下文由 **Provider 子树**解析。
- **inject 里的对象字段不会自动触发更新**；上下文应暴露 **`Signal` / `createDerived` 等**，在**返回的渲染函数**里用 `ctx.x()` 订阅。
- 提供方/消费方遵守 **主体内不解构 `props`**（见 props 规则）。

**完整说明、示例与参考实现**：[docs/agent-guidelines.md#viewfly-dependency-injection](../../docs/agent-guidelines.md#viewfly-dependency-injection)。
