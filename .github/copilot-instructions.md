# GitHub Copilot 项目说明

本组件库（viewfly-ui）的编码与架构约定写在仓库根目录的 **`docs/agent-guidelines.md`**。

编写或修改 `packages/**/*.tsx` 与 `packages/**/*.{tsx,scss}` 时，请遵循该文档中的：Viewfly 依赖注入与 Signal、Props 响应性、`createSignal`/`reactive`、`JSXNode` 类型，以及 UnoCSS + SCSS 与 `vfui-` 语义类名约定。

完整目录与代码示例见上述 Markdown 文件内各章节标题。
